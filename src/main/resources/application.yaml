spring:
  application:
    name: inventoryService
  security:
    oauth2:
      resourceserver:
        jwt:
          # Keycloak 공개키(JWK) 엔드포인트. jwk-set-uri는 지연 로딩이라 기동 시 네트워크 호출이 없다.
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:8080/realms/erp/protocol/openid-connect/certs}
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:inventory}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
